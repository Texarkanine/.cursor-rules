---
alwaysApply: true
---

```mermaid
graph TD
    A[Start] --> B[Check for Memory Bank file:]
    B --> C{activeContext.md exists?}
    C --"Yes"--> D["Memory Bank is already fully initialized<br>work is in-progress"]
    C --"No"--> E{systemPatterns.md exists?}
    E --"Yes"--> F["Memory Bank is partially initialized:<br>ready for new work"]
    E --"No"--> G["Memory bank is uninitialized"]
```

# Memory Bank Initialization

## Uninitialized

If the memory bank is completely uninitialized, the following persistent files must be created:

* `memory-bank/productContext.md`
    * The "Product Context File" is the business context of this collection of files: target users, use cases, success criteria, constraints. If this information is not obvious from traditional documentation sources, prompt the user for more information.
    * Load: `.cursor/rules/shared/niko/core/memory-bank/productContext.mdc` and create the file by following the instructions in the rule.
* `memory-bank/tasks.md`
    * The "Tasks File" is the active task tracking: current task details, checklists, component lists.
    * Load: `.cursor/rules/shared/niko/core/memory-bank/tasks.mdc` and create the file by following the instructions in the rule.
* `memory-bank/systemPatterns.md`
    * The "System Patterns File" is the architectural patterns of this collection of files: code organization, naming conventions, design patterns in use. Scan the codebase for significant patterns (nonstandard or high-criticality) and document them.
    * Load: `.cursor/rules/shared/niko/core/memory-bank/systemPatterns.mdc` and create the file by following the instructions in the rule.
* `memory-bank/techContext.md`
    * The "Tech Context File" summarizes how to work with the technology stack(s) in use in the project. Identify the tools, commands, and frameworks that should be top-of-mind while working on the project. Do NOT include session-specific info (current branch, current task, local environment, etc).
    * Load: `.cursor/rules/shared/niko/core/memory-bank/techContext.mdc` and create the file by following the instructions in the rule.
* `memory-bank/style-guide.md`
    * The "Style Guide File" names any style conventions that must be followed by the developer due to *not* being enforced by tooling: formatting, naming, documentation conventions specific to this repository. This file should be a summary of the conventions used in the project with references to the canonical source of truth for the project's conventions.
    * Load: `.cursor/rules/shared/niko/core/memory-bank/style-guide.mdc` and create the file by following the instructions in the rule.

## Partially Initialized - Ready for New Work

Once the memory bank has been partially initialized with the persistent files and is ready for new work, the following ephemeral files must be created for that work:

* `memory-bank/projectbrief.md`
    * The "Project Brief File" is the current session deliverable: user story & requirements.
    * Load: `.cursor/rules/shared/niko/core/memory-bank/projectBrief.mdc` and create the file by following the instructions in the rule.
* `memory-bank/activeContext.md`
    * The "Active Context File" is the current session focus: what's being worked on now, recent decisions, immediate next steps.
    * Load: `.cursor/rules/shared/niko/core/memory-bank/activeContext.mdc` and create the file by following the instructions in the rule.
* `memory-bank/tasks.md`
    * The "Tasks File" is the active task tracking: current task details, checklists, component lists.
    * Load: `.cursor/rules/shared/niko/core/memory-bank/tasks.mdc` and create the file by following the instructions in the rule.
* `memory-bank/progress.md`
    * The "Progress File" is the implementation progress: history of completed work and phase transitions.
    * Load: `.cursor/rules/shared/niko/core/memory-bank/progress.mdc` and create the file by following the instructions in the rule.
