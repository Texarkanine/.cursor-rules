---
alwaysApply: false
---

# Reconcile Persistent Files

> **TL;DR:** Quick scan of persistent memory bank files against the work just completed. Update only what this task invalidated. Skip silently if nothing changed.

## When This Runs

This rule is loaded at the end of every code-producing workflow:
- **L1**: During Wrap-Up, before the final commit
- **L2/L3**: During Reflect, after the reflection document is written, before the commit

## Persistent Files to Check

For each file, the linked guidance rule defines what belongs in it and how to write it. Follow that rule's conventions when making updates.

| File | Guidance Rule |
|------|---------------|
| `memory-bank/productContext.md` | `.cursor/rules/shared/niko/memory-bank/productContext.mdc` |
| `memory-bank/systemPatterns.md` | `.cursor/rules/shared/niko/memory-bank/systemPatterns.mdc` |
| `memory-bank/techContext.md` | `.cursor/rules/shared/niko/memory-bank/techContext.mdc` |

## Procedure

For each persistent file listed above:

1. **Read** the file's current contents.
2. **Compare** against the work just completed: does the file contain anything that is now **factually wrong** or **materially incomplete** because of the changes made in this task?
3. **If no**: move on — do not touch the file.
4. **If yes**: make a **surgical update** — fix only the specific content invalidated by this task.

If any file was updated, briefly note what changed and why in your output to the operator.

## Guardrails

- **Selective, not routine.** Most tasks won't change persistent files. This step should be a quick mental scan, not a ritual rewrite. If nothing is obviously wrong or missing, move on immediately.
- **Surgical, not comprehensive.** Update what this task invalidated. Do not audit for unrelated staleness. Do not rewrite sections that aren't directly affected.
- **Respect existing guidance.** When making updates, follow the linked guidance rule's conventions for the file being updated. Do not expand a file's scope beyond what its rule defines.
- **No scope creep.** Persistent files capture project-level knowledge, not task-specific details. A new React component doesn't warrant a `techContext.md` update. A migration from Jest to Vitest does.
- **Skip confidently.** When in doubt, leave the file alone. Under-updating is safe (future tasks can fix it). Over-updating introduces noise and potential inaccuracy.
