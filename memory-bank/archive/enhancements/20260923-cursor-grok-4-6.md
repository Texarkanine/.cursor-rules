---
task_id: cursor-grok-4-6
complexity_level: 2
date: 2026-09-23
status: completed
---

# TASK ARCHIVE: Add cursor-grok-4.6-xhigh to the verification catalog

## SUMMARY

Added `cursor-grok-4.6-xhigh` to the choose-verification-model catalog so `pick.py` accepts that slug and `cursor-grok-4.6-xhigh-fast`. Tier A, family grok, BenchLM mean 76.5, base output cost $6, `has_fast` true. The fast price ($12) is not stored. Pull request: https://github.com/Texarkanine/.cursor-rules/pull/128

## REQUIREMENTS

- Catalog slug `cursor-grok-4.6-xhigh`. The fast spelling is a suffix, not its own row.
- `has_fast` true because the pricing page has `Grok 4.6 (Fast)`. Rank on the $6 base price.
- BenchLM slug `grok-4-6`. Family `grok`. Hand-set tier A.
- Do not retarget `claude-opus-5-5-high` or `grok-4.7-xhigh`. Edit `rules/choose-verification-model/` only.

## IMPLEMENTATION

One mapping row, then `refresh.py`, then tier A on the new catalog entry. No selector change. `refresh.py` left every existing catalog row untouched. A second refresh kept tier A.

Files: `rules/choose-verification-model/assets/mapping.json`, `rules/choose-verification-model/assets/catalog.json`, `tests/choose-verification-model/test_shipped.py`.

## TESTING

New shipped tests went red on `unknown slug: cursor-grok-4.6-xhigh`, then green. A fast Sol author prints `cursor-grok-4.6-xhigh-fast`. A non-fast Sol author prints `cursor-grok-4.6-xhigh`. Opus stays Opus, so the row is not tier S. `grok-4.7-high` does not select this row. `make test`: 36 tests OK. QA passed with no rework.

## LESSONS LEARNED

A same-tier author plus one other row accepts both tier A (the rank window) and tier S (the one-tier lookup). A top-tier author, with nowhere to look up, is what rejects S. The Sol case and the Opus case together pin tier A without asserting the letter.

The first unknown slug in an argv is not the only one. After this row exists, the reported command fails on `claude-opus-5-5-high`, then `grok-4.7-xhigh`.

## PROCESS IMPROVEMENTS

Nothing notable. The Level 2 path matched the work: one executable step, tests red then green, no plan rewrite.

## TECHNICAL IMPROVEMENTS

Putting tier on the mapping row would let `build_catalog` finish a new slug without a hand edit. Preflight recorded that as a separate task. It was not built here.

## NEXT STEPS

`claude-opus-5-5-high` and `grok-4.7-xhigh` are still unknown. The catalog keys are `claude-opus-5-5-medium` and `grok-4.7-high`. The installed skill copy does not change until ai-rizz sync after this lands on the remote the tool reads.
