PASS

## Findings

1. **Resolved: `memory-bank/techContext.md` Testing Process now names the unittest suite.** The previous FAIL (fixable) is closed. The section now says `make test` runs the rulesets layout checks *and* a stdlib `unittest` suite discovered from `tests/`, that PR CI (`.github/workflows/rulesets-links.yml`) invokes the same Make targets, and that the unittest job sets up the Python version named in that workflow. That is the surgical fix the file's own update rule asked for: a developer reading this file now learns that Python/`unittest` is part of `make test`, and that CI has a Python setup step, without hardcoding `3.11`. Does not block.

## Verified clean

- `select()` still implements dense rank, the different-family window through one rank below, the one-tier-up cheapest-different-family then cheapest fallback, and every exit-2 case (unknown author, unknown reviewer slug, null-tier/null-score author, empty window at the top tier). Traced against the plan's pick behaviors and all 14 `test_pick.py` cases.
- `build_catalog()` still means the three BenchLM categories with missing-category dropout, keeps/replaces interim scores, preserves `tier_order` and existing tiers, warns on a new null tier, parses Output by header (including `[Name](url)` and reordered columns), and applies `output_multiplier`. Traced against all 11 `test_refresh.py` cases including refresh-then-pick.
- Shipped `catalog.json`/`mapping.json`: nine spawnable slugs, `tier: "general"`, non-null `score_source: "benchlm"`, Composer slugs share `composer-2-5` and keep independent prices. `test_shipped.py` does not lock numeric scores.
- All nine spawn sites carry the replacement sentence. No `python3`/`py` on those lines. No leftover "prefer smarter / different family" in `rulesets/` or `rules/`.
- README Subagent Selection and the supplementary-rules bullet match the plan. Symlink `rulesets/niko/skills/choose-verification-model` → `../../../rules/choose-verification-model`.
- CI job `choose-verification-model` checks out, sets up Python 3.11, and runs `make test-choose-verification-model`.
- `pick.py`/`refresh.py` are executable with shebangs. `modelpool.py` is not. `__pycache__/` is gitignored. `make test` is green (27 tests plus both layout checks).
- No TODOs, debug leftovers, or edits under `.cursor/` / `.claude/`.

## Advisory items

1. **Untested empty next tier.** If the one-tier-up fallback's next tier has no enabled members, `select()` raises `SelectionError`. Sound and safe, not in the Test Plan, and none of the nine shipped slugs can hit it today. Non-blocking.
