# Active Context

## Current Task: verification-subagents-preflight-qa

**Phase:** BUILD - IN PROGRESS (rework pass 2)

## What Was Done

- Preflight Spawn PASS WITH ADVISORY; plan amended in place (12 FAIL(TDD) sites enumerated; step 3 → tasks.md template Tests first with escape hatch; step 4 operator-gated)
- Operator `/niko-build` correctly bounced through fresh preflight (stale pass-1 status cleared)

## Next Step

1. Stockroom archaeology (advisory; do not block step 3)
2. Remove `FAIL (TDD)` species; tighten Plan template; cheap fixups
3. Ask operator on QA-status clear wording + README long QA split (mermaid)
