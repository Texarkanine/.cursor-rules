# Active Context

## Current Task: niko-plan-always-tdd
**Phase:** COMPLEXITY-ANALYSIS - COMPLETE

## What Was Done
- Intent confirmed: Plan (not the `niko-plan` router) must emit always-tdd ordering for executable units; preflight stays the hard gate; prose carve-out kept.
- Stockroom + two consults: decompression-key naming already lost to the `Files` / `Tests first` / `Changes` template; `always-tdd` is always-on local doctrine, not a pretrained key; Build already lists the four-step process, Plan does not.
- Classified Level 3 because activation (name vs load vs template-as-schedule) is a real design question before editing.

## Next Step
- Load Level 3 workflow and run Plan (open question → Creative).
