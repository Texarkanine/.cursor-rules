FAIL (blocking)

## Findings

1. **Completeness: the score source cannot score any enabled model (blocking).** Brief requirement 6 says scores come only from the SWE-bench `Test`, `Verified`, and `Multimodal` boards. On 2026-09-23 I fetched the pinned live `leaderboards.json` and checked every non-agent row that names a frontier model family. The newest row dates are `Test` 2025-12-19, `Verified` 2026-02-26, and `Multimodal` 2025-11-17. None of the models enabled for the Task tool today has a row on any board: Claude Opus 5.5, Claude Sonnet 5, Composer 2.5, Gemini 3.8 Flash, GPT-5.6 Terra, Grok 4.7, Kimi K3, and Muse Spark 1.3. The newest matching models on the boards are Claude 4.6 Opus, GPT 5.2, Gemini 3 Pro and 3 Flash, and Kimi K2.5. On `Test` and `Multimodal`, frontier models appear only in agent-prefixed rows, and the plan correctly excludes those. So the equal-weight mean over three boards is a mean over `Verified` alone for every model. Under the plan, unit 3 sets `swebench` only where a row names that model and effort. That leaves every shipped slug with `score: null`, so every one is out of the pool, and `pick.py` exits 2 on every call. Acceptance criterion 1 cannot be met. Unit 5 would then point all nine Niko Preflight and QA spawn sites at a command that always stops. This affects this repo and every consumer of the niko ruleset. The Pre-Mortem covers new slugs and null tiers, but not a score source that is seven months stale. The fix needs a brief-level decision from the operator. For example: (a) add a hand-set score in `mapping.json` for models the boards do not list, (b) use a different or additional benchmark source, (c) drop external scores and rank inside each tier by a hand-ordered list, or (d) ship the picker but do not do unit 5 until the scores exist. Each option changes requirement 6 or requirement 10, so the planner cannot choose one alone.
2. **Completeness: the captured slug list is not recorded (fixable).** Unit 3 step 4 and two Challenges and Pre-Mortem entries depend on "the model list captured for this task", but that list is not in `projectbrief.md`, `tasks.md`, or `progress.md`. Build cannot reproduce it. Fix: pin the slug list in `tasks.md`. For reference, the Task tool in this session lists `claude-opus-5-5-medium`, `claude-sonnet-5-thinking-high`, `composer-2.5`, `composer-2.5-fast`, `gemini-3.8-flash-high`, `gpt-5.6-terra-medium`, `grok-4.7-high`, `kimi-k3-high`, and `muse-spark-1.3-high`, plus `inherit`.
3. **Advisory: pricing rows use Markdown links.** Most model cells on the live pricing page are `[Claude Opus 5.5](https://…)`, and some are plain text, such as `Kimi K2.7 Code` and `Grok 4.6 (Fast)`. The page has two tables with different column widths. Every slug in the finding-2 list has a pricing row once the link text is extracted. The "Price" behavior should add a fixture row whose model cell is a link, and the parser should find the `Output` column by header in each table. If it does not, most shipped costs will be null and the refresh warnings will be the only signal.
4. **Advisory: tests ship to consumers.** `ai-rizz` installs an embedded skill with `cp -rL` of the whole directory, so `test_pick.py`, `test_refresh.py`, and `test_shipped.py` will go into every consumer's `.cursor/skills/`. This is harmless, but consider putting the tests outside the skill directory, for example in `tests/choose-verification-model/`, and pointing the Make target at it.
5. **Advisory: the spawn-line replacement sits inside a parenthetical.** At each site the old text is `Spawn a subagent (prefer smarter / different family if available);`. A literal swap puts a whole command inside the parentheses. Consider replacing the parentheses too, for example: `Spawn a subagent on the slug printed by pick.py beside the choose-verification-model SKILL.md (run with Python 3, passing …);`.
6. **Radical Innovation (advisory, not applied).** Drop the external score entirely. The operator already assigns tiers by hand, and `tier_order` is a hand-ordered list. Make each tier a hand-ordered list of slugs in `mapping.json`, best first, and define rank as position in that list. Then `refresh.py` does only prices. The SWE-bench fetch, effort parsing, name matching, latest-date selection, and renormalization are all removed, along with about half of the planned `test_refresh.py` behaviors. The "stale upstream" failure in finding 1 cannot happen, and one new model means one hand edit, which the null-tier warning already requires. The cost is that rank becomes operator judgment instead of a benchmark. That goes against requirement 6, so it needs operator buy-in. If you want benchmark input, refresh could print board scores as a suggestion without writing them.

## Verified as sound

- The last run's fixable findings are in the plan: effort comes from `reasoning_effort`, with the name parenthetical as fallback, and the Gemini 3 Pro case has a behavior. The leaderboards URL is pinned. The untested skipped-null warning is gone. The skill says to omit `inherit`, and an unknown author slug means stop.
- Live data matches the parser assumptions. `reasoning_effort` is a string only on `Verified` (11 `high`, 7 `medium`). Every row has a float `resolved` and a string ISO `date`. `Gemini 3 Pro` has effort `high` only in that field.
- TDD ordering: units 1 to 3 order stub tests, stub interface, red tests, then code. Units 4 to 6 are prose, policy, or CI and correctly have no tests. `test_shipped.py` is a contract test, not a change-detector. No swap or strike was needed.
- All nine spawn sites match the plan's list, confirmed by grep. `rulesets/niko/README.md:385` quotes the phrase and is covered by unit 5 step 2.
- Distribution: `ai-rizz` copies embedded skills with `cp -rL` after checking that each symlink stays inside the repo. The `rulesets/niko/skills/choose-verification-model -> ../../../rules/choose-verification-model` link follows the `illustrate-complexity` precedent and will ship `pick.py`, `refresh.py`, and both JSON files.
- The Makefile has only `test-symlinks` and `test-readme-links` today. The CI workflow has one job per target. `.gitignore` covers only `**/.summem/__pycache__/`. The plan's changes to all three fit these files.
