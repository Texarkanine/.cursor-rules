PASS WITH ADVISORY

TDD Plan Encoding: both units are prose/policy (skill wording + layout regression). No test steps required; none scheduled. No change-detector steps to strike. No executable unit lacking tests.

Convention: `rules/pr-review/SKILL.md` with `disable-model-invocation: true` and no ruleset matches `rules/pr-feedback-judge/` and `rules/wiggum-niko-coderabbit-pr/`. `REUSE.toml` already covers `rules/**/*.md` as PPL-S. Do not edit `.cursor/` or `.claude/`. `make test` only checks ruleset symlinks and ruleset README links, so a standalone `rules/` skill is a true no-op there.

Dependency: no consumers, tests, or ruleset READMEs need updates. Root README does not catalog unaffiliated `rules/` skills. Operator paste into the Cursor automation is out of repo, as planned.

Conflict: no existing `rules/pr-review/`. Distinct from `pr-feedback-judge` (comments in, dispositions out) and from `wiggum-niko-coderabbit-pr` (CodeRabbit loop). Description naming those neighbors belongs in frontmatter only; the paste body must stay free of sibling-prompt references.

Completeness: requirements 1–5 and acceptance 1–4 map to packaging + the numbered Other-sensitivity edits + the keep-unchanged list. Source text is the `/niko` message in the parent conversation (full current prompt, including posting shape, cascade, filters, cap, templates). Build copies that, then applies the listed edits — it must not invent the keep-unchanged sections.

Advisory — Build recoverability: if the parent context is compacted, recover the prompt from agent transcript 8a97504c-9b89-4254-938b-14c4df40f575 (the fenced `# PR Review` block in the opening user message). Do not reconstruct templates from the keep-unchanged bullet list alone.

Advisory — frontmatter vs paste: operator copies from the H1 down. A whole-file paste would carry neighbor names (`pr-feedback-judge`, ponytail-review) into the automation prompt. Write `description` for in-repo triggering per `prompt-authoring/references/skill-frontmatter.md` (when to act, not “Invoke with /pr-review”); stay under the spec’s description character limit.

Advisory — mermaid: quote node labels; keep the chart as map only (hunt → cull → sort → post, including the Other pass and early abort). Numbered prose remains the driving instructions.

Advisory — radical (not applied): Progress already says the live miss is cull-and-personality, not hunt-absence. A tighter prompt would keep the approve-gate duty and the split omit, and fold the closed Other list into Q3’s Other branch (happy-path edges, lockstep, tests that would still pass) instead of a second post-cascade workflow that can compete with the cascade. Cost: the explicit “second chance after Q3 yes” goes away; the gate becomes a five-bullet check before approve. The plan as written is still buildable; this is an optional redesign.
