PASS WITH ADVISORY

Preflight for `niko-core-compression` (Level 2), run 2026-09-02 against the revised `memory-bank/active/tasks.md`.

TDD Plan Encoding — PASS. The plan's single unit delivers agent rule wording, a prose/policy artifact outside `always-tdd.mdc`'s executable-behavior scope. No tests are owed, no change-detectors are scheduled, and no test/production ordering change is needed.

Convention Compliance — PASS. The plan modifies the canonical `rules/niko-core.mdc`, not generated `.cursor/` or `.claude/` copies. `rulesets/niko/niko-core.mdc` is a symlink to this source; the planned `make test` covers its integrity. The plan also explicitly requires no hard-wrapping.

Dependency Impact and Conflict Detection — PASS. The only consumer reference, `rulesets/niko/README.md`, links the file rather than individual sections. The named material slated for removal appears only in the target rule and active planning records. The plan introduces no duplicate implementation and preserves the existing intended division between `niko-core.mdc`'s TDD principle and `always-tdd.mdc`'s process.

Completeness Precheck — PASS. The revised plan explicitly maps every requirement: it retains all named bedrock invariants, consolidates context/ambiguity, error-handling, safety/approval, and communication guidance, removes every five named bloat bullets plus the two obsolete sections, folds Verification Reporting into Communication, and schedules `make test`.

Informational — this repository's injected `.cursor/rules/shared/niko-core.mdc` is an intentionally lagging generated copy. The change will not affect local injection until the source is pushed and a separate `chore(dev): ai-rizz sync` commit updates generated files. The plan correctly excludes that separate release action.

Advisory (Radical Innovation, not to be applied) — a future Level 3 redesign could replace prose sections with a concise set of stable, addressable invariants and a non-injected reference document. This would reduce always-injected token cost and let workflows cite invariant IDs, but must be weighed against the repository's intentional grep-verifiable duplication pattern.
