PASS WITH ADVISORY

Preflight run 2026-09-23, task cursor-grok-4-6 (Level 2). Plan validated against codebase reality.

Checks:
- TDD Plan Encoding: PASS. The single executable unit (shipped acceptance of the new slug) orders its steps stub tests -> stub interface -> write tests and run red -> write code and run green. Test-writing precedes production code unambiguously. No change-detector or non-contract steps to strike.
- Convention Compliance: PASS. Edits land in `rules/choose-verification-model/` (canonical source, per systemPatterns) and `tests/choose-verification-model/` (existing unittest discovery). Import mechanism (`sys.path.insert` + `from modelpool import select`) matches `test_pick.py` exactly.
- Dependency Impact: PASS. All slugs the test plan references exist with the assumed tiers: `gpt-5.6-sol-medium` (A, has_fast), `claude-opus-5-5-medium` (S), `grok-4.7-high` (A, family grok). Existing `test_shipped.py` loops stay green: the new row gets a ladder tier and a non-null score before tests run. `refresh.py` behavior matches the plan's expectations (writes `tier: null` + `must set tier` warning for new slugs; keeps hand-set tiers on re-refresh).
- Conflict Detection: PASS. `cursor-grok-4.6-xhigh` is absent from mapping and catalog; no duplication-in-waiting. `canonical_slug`/`_with_speed` already implement the `-fast` suffix rule, so no selector change is needed — the plan correctly adds none. Out-of-scope slugs `claude-opus-5-5-high` and `grok-4.7-xhigh` are indeed unknown today.
- Completeness: PASS. Every brief requirement maps to a concrete step, and the four selection tests plus the SPAWNING membership loop cover the new observable behavior. Window math verified against `select`: with only Sol and the new row enabled, the two-row rank window always includes the row iff it is tier A; the Opus case excludes tier S; the grok same-family case fails closed on a `cursor` family. Together these pin tier A without asserting the letter, as designed.

Advisory (radical innovation, non-blocking): the refresh workflow has a recurring manual step — refresh writes `tier: null` for a new slug, a human hand-edits the tier into `catalog.json`, and a second refresh preserves it. Consider adding an optional `"tier"` field to the mapping row and having `build_catalog` resolve tier as `previous tier ?? mapping tier ?? null`. The catalog then becomes fully regenerable from mapping + upstream data, the hand-edit step disappears for every future model addition, and the "hand-edited value drifts" pre-mortem class shrinks to nothing. Cost: a small `build_catalog` change plus tests; it touches the refresh contract, so it is a separate task, not this one.

Advisory (minor): the plan's red-run expectation and its stop conditions depend on BenchLM having `grok-4-6` and the pricing page listing `Grok 4.6` / `Grok 4.6 (Fast)`. A cheap `curl` of both URLs before writing tests would confirm the external inputs exist; the plan already handles their absence correctly (stop and report, invent nothing), so this is sequencing comfort only.
