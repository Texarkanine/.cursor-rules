PASS WITH ADVISORY

Preflight run 2026-09-13, task speedup-giesen (Level 2). All seven default-preflight checks passed; one advisory finding.

Checks:
1. Wrong-reference guard: progress.md records Level 2; correct reference loaded.
2. Prerequisites: planning complete; all tasks.md status boxes through Pre-Mortem checked.
3. TDD Plan Encoding: the single implementation unit delivers skill wording (prose/policy), which owes no tests under always-tdd. The plan correctly omits tests and explicitly forbids change-detector wording assertions. Pass.
4. Convention Compliance: `rules/speedup-giesen/SKILL.md` follows the a-la-carte topic-skill pattern with verified precedent (`rules/xy-problem/SKILL.md`: name + description frontmatter, no disable-model-invocation, no ruleset symlink). REUSE.toml confirmed to cover `rules/**/*.md` as LicenseRef-PPL-S, so no licensing edit is needed. Pass.
5. Dependency Impact: both `make test` scripts (check-ruleset-symlinks.sh, check-ruleset-readme-links.sh) were read; they scan only the `rulesets/` tree. A new a-la-carte directory under `rules/` with no symlink and no README link is invisible to them, so the plan's "expect PASS with no new coverage" claim holds. Pass.
6. Conflict Detection: no existing performance/speedup skill under `rules/`. The ponytail plugin (external to this repo) is the named near-neighbor and the plan's description trigger explicitly disclaims it. No public contract is touched. Pass.
7. Completeness: brief requirements 1-4 and acceptance criteria 1-4 each map to a concrete plan step (file path, frontmatter shape, verbatim quote text with URL, six-step passover, make-test verification). Pass.

Advisory (radical innovation, non-blocking): the plan's passover classifies cuts as 100x-stupid vs 2x-smart but never requires measuring anything. Giesen's claim is quantitative; without a measurement the class label is an assertion, and the stop-and-report branch ("only 2x-smart candidates found") has no numbers to report. Structural sketch: insert one numbered step between plan steps 5 and 6 - "Measure the named surface before and after each cut (wall time on the operator-named path, or a count of the repeated job); report the observed multiplier beside the class label." One step, no new machinery in the target product, and it makes "we stopped doing something stupid" evidence rather than vibe. Operator may weigh this against the cost of a plan edit; the plan is buildable as-is.
