FAIL (fixable)

Preflight for `niko-core-compression` (Level 2), run 2026-09-02 against `memory-bank/active/tasks.md`.

TDD Plan Encoding — PASS. The plan's single implementable unit ("Refactor and compress `rules/niko-core.mdc` — prose/policy") delivers agent rule wording, which `always-tdd.mdc` places outside TDD scope, so no test steps are owed and none are missing. No scheduled step asserts on headings, phrases, links, or checklists in a document, so there was no change-detector to strike and no test/production step ordering to swap. No in-phase edits were made to `tasks.md`.

Convention Compliance — PASS. The plan targets the canonical `rules/niko-core.mdc` and never the generated `.cursor/` or `.claude/` trees, matching `agent-customization-locations.mdc` and `memory-bank/systemPatterns.md`. `rulesets/niko/niko-core.mdc` is a symlink to `../../rules/niko-core.mdc`, so editing the canonical path is sufficient and the symlink is left intact for `make test-symlinks`. Substep 2's final item correctly binds the work to `markdown-style.mdc`'s no-hard-wrap requirement.

Dependency Impact — PASS with one informational note. Nothing outside `rules/niko-core.mdc` references any of the bullets or sections slated for removal; a repo-wide search for "Proactive Foresight", "Propose Enhancements", "Reusability Mindset", "Evaluate Strategies", "Strict Rule Adherence", and "Ensure Production-Ready Quality" matches only `rules/niko-core.mdc` itself. `rulesets/niko/README.md` line 20 links the file, not any section, so `make test-readme-links` is unaffected. Informational: the generated copy at `.cursor/rules/shared/niko-core.mdc` is what is actually injected in this repo, and per `systemPatterns.md` it cannot be re-synced in this task — the compressed rule will not govern any agent, including agents working in this repo, until the change is pushed and a separate `chore(dev): ai-rizz sync` commit lands. The plan is right to omit the sync; the operator should simply expect the lag.

Conflict Detection — PASS. The plan builds nothing the repo already provides and breaks no published contract. The retained TDD directives coexist with the dedicated `always-tdd.mdc` always-applied rule by design (niko-core states the principle, `always-tdd.mdc` holds the process), and that overlap is unchanged by the plan. The `<core_operating_principles>` wrapper tag and the `Progress Tracking` bullet were already removed from the canonical file by the operator in commit a737658; the plan does not disturb either, so no unintended restoration or re-removal is scheduled.

Completeness Precheck — FAIL (fixable). Requirement 3 of `projectbrief.md` ("Cut Redundant Internal Bloat") names five specific deletions. The plan's substep 2 maps only two of them: "Remove standalone `## Error Handling` and `## Proactive Foresight & System Health` sections". It has no step for removing the `Propose Enhancements` bullet (a Research & Planning bullet at line 28, distinct from the `## Proactive Foresight & System Health` section), nor for `Reusability Mindset` (line 26), `Evaluate Strategies` (line 27), `Strict Rule Adherence` (line 37), or `Ensure Production-Ready Quality` (line 49). Substep 2 does enumerate what to *retain* under Research & Planning and Verification & Quality Assurance, which makes the omission actively misleading: an implementer following the plan literally would keep all four filler bullets and `Propose Enhancements`, and the brief's stated anti-scope-creep goal would be only half met. Fix: add explicit removal substeps for the five named bullets so every requirement-3 deletion has a step. Requirements 1, 2, and 4 are each fully mapped to concrete substeps.

Advisory — the plan consolidates Communication into a single "Structured, Low-Overhead Updates" bullet but leaves `Verification Reporting` in place under Verification & Quality Assurance, and the two say close to the same thing ("succinctly describe verification steps" vs. "report actions, changes, verification findings"). Consider folding `Verification Reporting` into the consolidated communication bullet rather than leaving a near-duplicate in a different section; the brief's deduplication goal reaches it even though requirement 3 does not name it.

Advisory (Radical Innovation, not to be applied) — the highest-leverage structural change available here is to stop treating niko-core as prose and turn it into an addressable constitution. Concretely: reduce `rules/niko-core.mdc` to frontmatter plus roughly ten numbered invariants with stable IDs (`C1` collaborator posture, `C2` clean-break default and public-interface identification, `C3` TDD, `C4` test integrity, `C5` root-cause resolution and the >2-attempt circuit breaker, `C6` autonomous execution thresholds, `C7` credential security, and so on), one sentence each, and move today's elaborative paragraphs into a non-injected companion under `rules/niko-core/references/` that an agent reads only when an invariant is contested. Stable IDs would let every Niko workflow, preflight gate, and QA check cite `C3` instead of restating the TDD rule in its own words, which is precisely the drift risk `systemPatterns.md` warns about for a self-hosting repo, while the always-injected payload drops to well under a page. The tension to weigh before adopting it: `systemPatterns.md` also documents verbatim duplication as a deliberate grep-verifiable tripwire, so an ID-citation scheme would have to be evaluated against that pattern rather than assumed to supersede it — and the change is a Level 3 redesign, not this task's Level 2 compression.
