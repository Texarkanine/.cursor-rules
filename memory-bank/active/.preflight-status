FAIL (fixable)

## Findings

1. **Completeness / correctness (fixable)** — The plan reads a row's effort only from a trailing name parenthetical (Invariants: "Match a board row by exact model name after removing ... one effort parenthetical"; behavior "Effort isolation"). Live `leaderboards.json` rows carry a structured `reasoning_effort` field, and the two disagree: Verified has a row named `Gemini 3 Pro` (no parenthetical) with `reasoning_effort: "high"`. Under the plan, that row counts as untagged, so a null-effort mapping for Gemini 3 Pro would receive a `high` score, and a `high` mapping would get nothing. That breaks brief requirement 7 ("A score is not copied onto an effort the bench did not name"). Fix: take effort from `reasoning_effort` when it is non-null, and fall back to the name parenthetical otherwise. Keep the name-parenthetical stripping for the exact-name match. Add a behavior with an untagged name, `reasoning_effort: "high"`, and a null-effort mapping that does not receive the row.
2. **Test coverage gap (fixable, minor)** — Unit 1 step 4 implements "stderr warnings for nulls that were skipped only when they were the reason a fallback missed", but no entry under Behaviors to Verify covers it, so it would ship untested. Fix: add a behavior (the fallback misses because the only candidate has a null cost or tier → stderr names that slug) or drop the warning from step 4.
3. **Dependency Impact (fixable, minor)** — `refresh.py` fetches "SWE-bench `leaderboards.json`" with no URL, while the pricing source is pinned. `https://raw.githubusercontent.com/SWE-bench/swe-bench.github.io/master/data/leaderboards.json` returns 200 today, with top-level `leaderboards[]`, each holding `name` and `results[]` whose rows have `name`, `resolved`, `date`, and `reasoning_effort`. Fix: pin that URL (or the one the planner prefers) in the plan and in `refresh.py`.
4. **Advisory: rollout order** — Tiers ship null, so once unit 5 lands and `ai-rizz sync` runs, every Niko Preflight and QA spawn exits 2 and stops until the operator fills tiers. This affects this repo and every consumer of the niko ruleset. The Pre-Mortem accepts this as the hand-off, but nothing requires the null state to reach `main`. Consider having the operator fill `tier_order` and tiers in the same PR before merge, or landing unit 5 in a follow-up PR once tiers exist.
5. **Advisory: argv hazards in the skill text** — The Task-tool list includes `inherit`, which is not a model slug. Passed through, it hits the unknown-slug exit 2. An agent also may not know its own exact slug, for example the effort suffix. `SKILL.md` should tell the agent to omit `inherit`, and should state that an unknown author slug means stop and report, not guess.
6. **Radical Innovation (advisory, not applied)** — Move hand-assigned tiers out of the shipped `catalog.json` into an operator-owned overlay, for example `tiers.json` beside the skill, with the shipped copy as the default. `refresh.py` would then write only scores and prices and never touch the tier file, and `pick.py` would merge the two. This makes "refresh preserves tiers" a structural guarantee instead of a merge rule, and it removes a class of refresh bugs and tests. It also means that re-running refresh, or regenerating the catalog from scratch, cannot clobber tier judgments. A consumer could keep a local tier file that an `ai-rizz` update of the catalog does not overwrite. Cost: one more file and a merge step in `modelpool.py`. This goes beyond brief requirement 9's wording, so it needs operator buy-in.

## Verified as sound

- TDD ordering: units 1–3 are product-delivered executables (agents in consumer projects run `pick.py` and `refresh.py`, and a user sees a wrong pick). Each unit orders stub tests, stub interface, red tests, then code. No swap or strike was needed.
- `test_shipped.py` is a contract test, not a change-detector. `pick.py` reads both JSON files at runtime, so a refresh or hand edit that desyncs them breaks users, and the test goes red.
- Units 4–6 (skill prose, spawn lines, README, CI YAML) are prose/policy and correctly carry no tests.
- All nine spawn sites re-confirmed by grep against `prefer smarter / different family if available`. `rulesets/niko/README.md:385` quotes that phrase, and unit 5 step 2 rewrites that section.
- The previous run's fixable findings are addressed: a CI job in unit 6, `.gitignore` `__pycache__/` in unit 1, spawn lines that say "Python 3" with `SKILL.md` giving `python3` and `py -3`, and a README supplementary bullet in the same shape as `illustrate-complexity` at README line 25. The readme link checker accepts relative `../../rules/...` targets.
- The symlink `rulesets/niko/skills/illustrate-complexity -> ../../../rules/illustrate-complexity` is the exact precedent for the new link.
- The pricing page `https://cursor.com/docs/models-and-pricing.md` returns 200. It has an `Output` column and separate `(Fast)` rows, which `pricing_name` plus `output_multiplier` handles.
- Boards named `Test`, `Verified`, and `Multimodal` exist in live data with a `resolved` value and a `date` on each row.
