PASS WITH ADVISORY

Preflight run 2026-09-23 for task effort-variant-slug, rework plan (Level 2). All seven default-preflight checks ran; none failed. No in-phase plan edits (no TDD swap, no strike) were needed: both executable units already order stub-tests before stub-interface before write-tests-run-red before write-code-run-green, and no scheduled change-detectors were found.

Check results:

1. Wrong-reference guard: progress.md records Level 2; the default reference is correct.
2. Prerequisites: tasks.md shows initialization, test planning, implementation plan, technology validation, and pre-mortem complete. Level 2, so no creative-phase documents required.
3. TDD Plan Encoding (blocking): PASS. Unit 1 (benchlm_scores) and unit 2 (place the author, then the existing window) each schedule stub tests, stub interface, red tests, then green code, explicit enough that coding first is not a readable interpretation. Unit 3 (refresh.md) is prose/policy and correctly owes no tests. No numbered step is a change-detector (no heading, phrase, link, or checklist assertions on a document) and no non-contract lockstep is scheduled.
4. Convention Compliance: PASS. All edits land in canonical rules/choose-verification-model/scripts/, assets/, references/ and tests/choose-verification-model/, never the generated .cursor/ copies, matching systemPatterns.md. benchlm_scores follows the existing snake_case helper naming; scores.json follows the catalog.json/mapping.json asset pattern. select gains an optional scores=None parameter, which preserves the existing five-argument call shape.
5. Dependency Impact: PASS with one advisory (A below). build_catalog keeps its signature and (catalog, warnings) return, so refresh.main and test_refresh.py call shapes hold. select consumers are pick.main, test_pick.py, and test_shipped.py; the intended behavior change (an outside author with a wide score is now placed instead of echoed) is the brief's Rework item 3 and its test rewrites are scheduled in unit 2. The plan accounts for the null-tier/null-score exit-2 regression, the thinking stem, and the shipped issue command.
6. Conflict Detection: PASS. No existing wide-score helper; benchlm_scores composes with the existing _category_values mean logic rather than duplicating a shipped utility. The wide-score-must-not-become-a-reviewer rule (enabled slugs still have to be catalog keys, exit 2) preserves the published exit-2 contract for reviewers.
7. Completeness Precheck: PASS. All five brief Rework items map to concrete units: wide mean to unit 1; tiered-subset catalog to unit 1; placement plus boundary-keeps-higher-tier plus existing window to unit 2; echo-without-score and exit-2-for-non-catalog-enabled to unit 2; one-effort-per-model printing to units 2 and 3. All eleven Behaviors to Verify have named test homes in test_refresh.py, test_pick.py, or test_shipped.py.

Advisory findings (do not block):

A. Test hermeticity for the new scores input. Unit 2 says pick.main loads assets/scores.json when scores is omitted, but the existing _run helper in test_pick.py injects only catalog and mapping, so every main-level test would read the shipped scores.json from disk. Fictitious slugs will miss the lookup and behave as today, so nothing breaks, but the suite stops being hermetic. Sketch: thread an optional scores argument through main the same way catalog and mapping are threaded, defaulting to the asset file only in production; likewise write scores.json next to dest rather than a fixed path in refresh so injected-dest tests do not touch the working tree.
B. Placement distance is pinned by example rather than stated as a rule. The stub docstring says the tier comes from the catalog score-neighbors with the boundary keeping the higher tier, and the 10/40/20-goes-A, 5-stays-B, boundary-goes-A cases pin three points, but the metric (nearest neighbor score, with ties up) is never written as one sentence. One clause in unit 2 would make the coded rule match the examples under any future catalog.
C. An unmapped author's BenchLM slug is found by model_key equality, so a Cursor stem whose BenchLM spelling differs in punctuation (dots versus dashes) misses the wide table and echoes with exit 0. That is the safe, brief-consistent default, and the pre-mortem already names this dead-in-the-water case, so it is advisory only. If the operator wants fewer echoes, the sketch is a reverse lookup through mapping benchlm_slug values plus a punctuation-normalized fallback before giving up.

Radical-innovation advisory (Step 4; not a defect, no plan change made): precompute placement instead of shipping a wide table. Structural sketch for the operator to evaluate: have refresh write assets/placement.json mapping each BenchLM slug to its placed tier (and nearest-neighbor distance) against the current catalog, so pick becomes a dict lookup plus the existing window with no numeric logic at runtime. It removes a runtime scoring path and makes tier drift visible in reviewable JSON, at the cost of a second asset to keep in sync and a fallback rule when the asset predates a catalog hand-edit. Kept as-is, scores.json plus in-memory placement is the smaller diff and the right default; this is a judgment call.
