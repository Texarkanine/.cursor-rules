PASS WITH ADVISORY

Prerequisites: Level 2 plan complete; creative doc present and its decision (E, onboard don't place) is what the plan implements. No in-phase plan edits.

TDD encoding: units 1 and 2 are executable (a user sees `pick.py` exit 2 on `claude-opus-5-5-max`; refresh output is operator-visible) and both stub tests, write tests, run red, then code. Unit 3 is data covered by existing `test_shipped.py` invariants. Unit 4 is prose/policy and owes no tests. No change-detectors scheduled.

Conventions: edits land in canonical `rules/choose-verification-model/` and `tests/choose-verification-model/`, not generated `.cursor/`. No new files, so no REUSE entries needed. Keyword-injection into `refresh.main` matches its existing `benchlm=`/`pricing_markdown=`/`dest=` pattern.

Dependency impact: no existing test calls `refresh.main`, so the new `agent_models=None` default (spawn `agent`) touches no current test. `pick.py` is unaffected apart from `model_key`, per constraint 5.

Completeness: requirements 1-8 each map to a unit; acceptance 1-6 map to behaviors or unit 3 step 5.

Advisory findings:
1. Hermeticity: any future `refresh.main` test that omits `agent_models` will spawn the real CLI (60 s timeout). Consider telling build to pass `agent_models` in every `main` test, or to default tests through a helper.
2. Unit 3 step 3 says rows with "no score" are unrecognized, while requirement 5 accepts an interim score. Step 2 fixes `interim_score: null`, so the two agree today. If build sets an interim score by hand, that row counts as recognized.
3. Vocabulary risk: a future stem whose real name ends in `-none`, `-max`, or `-minimal` would be split. The idempotence test and `test_shipped` guard shipped keys only, not listed stems. Refresh's unmapped warning would surface a mis-split stem, so the risk is visible, not silent.

Radical innovation (advisory, not applied): have refresh print a *suggested* mapping row for each unmapped stem. It would take the stem, normalize dots and dashes and word order (e.g. `claude-4.6-opus` → `claude-opus-4-6`), and look for exact candidates in the BenchLM slugs and pricing Model cells it already fetched. It would print candidates and write nothing. This turns 36 hand searches into 36 confirmations and keeps the no-fabrication rule, because the operator or build still accepts each match. Cost: one pure helper plus tests in unit 2.
