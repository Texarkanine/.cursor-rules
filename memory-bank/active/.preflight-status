PASS WITH ADVISORY

## Findings

1. **Blocking finding from the prior run is resolved.** Re-fetched the live, pinned `https://benchlm.ai/data/models.json` today. All nine spawnable slugs' BenchLM counterparts (`claude-opus-5-5`, `claude-sonnet-5`, `composer-2-5`, `gemini-3-8-flash`, `gpt-5-6-terra`, `grok-4-7`, `kimi-k3`, `muse-spark-1-3`) carry `scores.displayCategoryScores` with at least two of `agentic`/`coding`/`reasoning` populated, even where `displayScore`/`evidenceStatus`/`overallRank` are null (Muse Spark 1.3, Grok 4.7, Composer 2.5 — Composer 2.5 has `reasoning: null` but `agentic`/`coding` present, matching the plan's own claim). Every shipped slug gets a real mean, `interim_score` stays null for all nine as the plan specifies, and `pick.py` will not exit 2 on every call. Acceptance criterion 1 is achievable.
2. **All four prior fixable findings are in this revision.** The nine-slug list is now pinned verbatim in `tasks.md`'s Pinned Info table and matches this session's actual Task-tool slug list exactly. `test_refresh.py`'s planned behaviors include the Markdown-link Model cell and header-based Output-column lookup. Tests moved to `tests/choose-verification-model/`, outside the skill directory `ai-rizz` would `cp -rL`. Unit 5 step 1 now replaces the whole parenthetical with a sentence outside it, and explicitly avoids naming `python3`/`py` on the shared spawn lines.
3. **Re-verified independently, still sound:** `Makefile` has only `test-symlinks`/`test-readme-links` today (unit 1 adds a third target correctly); `.gitignore` ignores only `**/.summem/__pycache__/` (unit 1's addition is not redundant); `REUSE.toml`'s default `path = ["**/*"]` AGPL annotation covers new `.py`/`.json` files and its `rules/**/*.md` PPL-S annotation covers the new `SKILL.md`, so no `REUSE.toml` edit is needed as the plan states; `.github/workflows/rulesets-links.yml` has exactly one job per existing Make target, so unit 6's new job matches the file's own convention; all nine spawn sites (grepped fresh) match the plan's site list exactly, with no other in-repo canonical site missed; the `rulesets/niko/skills/choose-verification-model -> ../../../rules/choose-verification-model` symlink depth matches the `illustrate-complexity` precedent exactly, and the README bullet's `../../rules/...` depth matches too; every one of the nine pricing rows the mapping will need (`Claude Opus 5.5`, `Claude Sonnet 5`, `Composer 2.5`, `Composer 2.5 (Fast)`, `Gemini 3.8 Flash`, `GPT-5.6 Terra`, `Grok 4.7`, `Kimi K3`, `Muse Spark 1.3`) exists on the live pricing page fetched today.
4. **TDD ordering is correct.** Units 1–3 (executable: `pick.py`, `refresh.py`, the shipped JSON data) each order stub tests → stub interface → red tests → green code. Units 4–6 (`SKILL.md` prose, the nine spawn-line edits plus README, the CI job) are prose/policy or CI config, correctly carry no test steps, and are not executable behavior under `always-tdd.mdc`'s scope. `test_shipped.py`'s planned assertions (required keys present, non-empty `family`, non-null score/tier for the nine spawnable slugs) are behavioral invariants, not change-detectors — the plan explicitly declines to lock numeric scores. No swap or strike needed.
5. **No conflicting or duplicate implementation exists.** Grepped the repo for `modelpool`, `pick.py`, `refresh.py`, and any existing `tests/` directory or `.py` file: none exist outside this task's own memory-bank references. This is new ground, not duplication-in-waiting.

## Advisory items

1. **Untested edge case: a cataloged-but-null-tier author.** The plan's `--model` validation only specifies "unknown slug → exit 2" (finding 1's earlier fix) and separately excludes null-tier/null-score *candidates* from the reviewer pool. It never states what `select()` does if the author slug itself is present in `catalog.json` with `tier: null` (e.g., an operator ran `refresh.py`, which per the plan only touches slugs already in `mapping.json`, added a new mapping row, and the operator's own current model becomes that slug before assigning it a tier). This is narrow and self-inflicted — none of the nine shipped slugs can hit it — but worth one explicit sentence and test case in unit 1 (e.g., "a null-tier or null-score author is also an exit 2") so `select()`'s behavior isn't accidental.
2. **Pricing-name exactness for multi-variant rows.** The live pricing page lists four `Grok 4.7*` rows (`Grok 4.7`, `Grok 4.7 (Fast)`, `Grok 4.7 500k`, `Grok 4.7 500k (Fast)`) and two `Composer 2.5*` rows. Since `pricing_name` matching is exact-string (per the Price test behavior), this resolves cleanly as long as unit 3's implementer copies the exact row name — flagging only so the implementer doesn't reach for `output_multiplier` on `composer-2.5-fast` when the pricing page already has its own direct `Composer 2.5 (Fast)` row.

## Radical Innovation (advisory, not applied)

Add an optional `--json` flag to `pick.py` that, alongside the plain slug on stdout, can emit a one-line structured record of the decision — selected slug, the tier and rank window considered, and which fallback branch fired (window / one-tier-up-diff-family / one-tier-up-cheapest). Niko's spawn lines already write a Progress entry after each phase; if that entry captured this record verbatim, an operator could audit reviewer diversity and fallback frequency over many tasks without re-deriving it from `catalog.json` by hand — turning a one-shot selection into a queryable trail. Doesn't touch any pinned requirement; purely additive to `pick.py`'s CLI surface.
