PASS WITH ADVISORY

Preflight run 2026-09-23 for task effort-variant-slug (Level 2). All seven default-preflight checks ran; none failed. No in-phase plan edits (no TDD swap, no strike) were needed.

Check results:

1. Wrong-reference guard: progress.md records Level 2; default reference is correct.
2. Prerequisites: tasks.md shows initialization, test planning, implementation plan, technology validation, and pre-mortem complete. Level 2, so no creative-phase documents required.
3. TDD Plan Encoding (blocking): PASS. Both executable units (place_effort; select/_with_effort) order their substeps stub-tests -> stub-interface -> write-tests-run-red -> write-code-run-green, explicit enough that coding first is not a readable interpretation. Unit 3 (refresh.md note) is prose/policy and correctly owes no tests. No scheduled change-detectors: the shipped test asserts exit code, an allowed spelling, and that running the command leaves catalog.json bytes unchanged - a behavioral assertion about the program, not a lock on document content.
4. Convention Compliance: PASS. All edits land in canonical rules/choose-verification-model/ and tests/choose-verification-model/ trees, never the generated .cursor/ copies, matching systemPatterns.md. Tests reuse the existing stdlib unittest suite and the _entry/_catalog/_mapping/_run fixtures; no new test files, matching the suite's layout.
5. Dependency Impact: PASS with one advisory. Consumers of modelpool are pick.py (select, SelectionError), refresh.py (build_catalog only - untouched), test_pick.py, test_shipped.py, and test_refresh.py. The only existing test whose asserted behavior changes is test_unknown_author_slug_exits_2, and its rewrite is explicitly scheduled in unit 2. test_refresh.py and test_shipped.py call select with exact catalog keys, so their behavior is unchanged. The shipped-command fixture in the plan was verified against the shipped catalog: cursor-grok-4.6-xhigh is an exact key after -fast stripping; claude-opus-5-5-high and grok-4.7-xhigh each have exactly one same-stem effort-suffixed sibling, so both are placeable under the unit-1 rules.
6. Conflict Detection: PASS. No existing effort-placement utility; canonical_slug is the only slug normalization and the plan composes with it. The exit-2-to-exit-0 change for an unplaceable author is the intended, operator-approved contract change recorded in the brief and the amended plan; SKILL.md's "use the slug it prints; on non-zero exit, stop" remains consistent with it.
7. Completeness Precheck: PASS. All five brief requirements and all five acceptance criteria map to concrete plan units and named test behaviors, including the boundary-tier rule, the thinking-stem exclusion, the bare-key exclusion, caller-catalog immutability, and the fast-suffix interaction.

Advisory findings (do not block):

A. Sibling anchors and far rows should say "stored rows only." Unit 1's tier rule excludes synthetic neighbors ("other synthetics are not neighbors") and Challenges & Mitigations records "two synthetics in one pick are placed against stored rows only," but the sibling-selection sentence itself ("Siblings are catalog keys with the same stem that themselves end in an effort word...") does not exclude rows with score_source "effort" that _with_effort inserted earlier in the same pick. Read literally, placing m-xhigh after m-high was inserted would anchor on the synthetic and make placement insertion-order-dependent - the exact failure the recorded mitigation rules out. The plan read as a whole disambiguates this, so it is advisory; one clause ("siblings and far rows are stored rows; skip score_source == 'effort'") in unit 1 would make the coded sentence match the recorded intent.

B. Two docstrings go stale and are not scheduled. select's docstring and pick.py main's docstring both describe the unknown-author path as raising/exiting 2. After unit 2, an unplaceable author is printed with exit 0. Unit 3 updates references/refresh.md but not these two docstrings. A one-line docstring touch-up during unit 2 keeps the code's own documentation true.

C. Radical-innovation advisory: the both-siblings interpolation branch is unreachable with any catalog this product can refresh. Refresh never creates effort-variant rows and the hand-chosen catalog currently stores exactly one effort spelling per stem, so a requested effort can never have both a lower and an upper stored sibling outside fixtures. Structural sketch for the operator to evaluate: delete the two-sided branch and its fixture tests, and let the one-sided rules handle a hypothetical future multi-effort stem - with stored medium and xhigh, a requested high anchors on the lower sibling and interpolates toward the far other-stem row, which is still a sane, monotone placement. The cut removes roughly a third of the formula and its tests at the cost of re-adding the branch if a multi-effort stem is ever hand-added. Kept as-is, the branch is cheap, tested insurance; this is a judgment call, not a defect.
